apply plugin: 'java'

repositories {
    mavenCentral()
}


dependencies {
    // tag::jetty[]
    compile("org.springframework.boot:spring-boot-starter-actuator:1.2.7.RELEASE")

    testCompile("junit:junit")
}